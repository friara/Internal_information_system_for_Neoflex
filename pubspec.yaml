name: news_feed_neoflex
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.5.4

dependencies:
  flutter:
    sdk: flutter
  sqflite: ^2.0.0+4
  sqflite_common_ffi: ^2.2.0
  path_provider: ^2.0.2
  image_picker: ^1.1.2
  permission_handler: ^12.0.0+1
  file_picker: ^10.1.2
  syncfusion_flutter_pdfviewer: ^29.1.40
  open_file: ^3.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true  
  assets:
    - assets/images/
    - assets/images.json
    - assets/text/
    - assets/data.json
